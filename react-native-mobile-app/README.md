## Get started

1. Install dependencies

   ```bash
   npm install
   ```

2. Start the app

   ```bash
   npx expo start
   ```
   
   ```bash
   npx expo run:android --device
   ```

   ```bash
    cd android
    ./gradlew clean
    cd .. 
   npx react-native run-android

   ```